<% if object.errors.any? %> <!-- anyはemptyの逆-->
    <div id="error_explanation">
      <div class="alert alert-danger">
        The form contains <%= pluralize(object.errors.count, "error") %>.<!-- pluralizeはcountが1なら単数形、それ以外なら複数形に第二引数を整形してくれる-->
      </div>
      <ul>
        <% object.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>